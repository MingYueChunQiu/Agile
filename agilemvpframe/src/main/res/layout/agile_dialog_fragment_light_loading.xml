<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_agile_dfg_light_loading_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true"
    android:focusableInTouchMode="true">
    <!--设置点击焦点属性，拦截点击触摸事件，防止事件穿透到底层
        只针对下层为fragment有效，对直接放在父容器FrameLayout中的控件无效-->

    <android.support.v7.widget.LinearLayoutCompat
        android:id="@+id/ll_agile_dfg_light_loading_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/shape_dialog"
        android:elevation="@dimen/dp_10"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/dp_20">

        <ProgressBar
            android:id="@+id/pb_agile_dfg_light_loading_progress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <android.support.v7.widget.AppCompatTextView
            android:id="@+id/tv_agile_dfg_light_loading_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/dp_20"
            android:text="@string/prompt_loading"
            android:textColor="@android:color/black" />
    </android.support.v7.widget.LinearLayoutCompat>

</RelativeLayout>